package proof

import (
	"github.com/berachain/beacon-kit/mod/errors"
)

// ErrTypeIsNotElements is returned when the type is not elements.
var ErrTypeIsNotElements = errors.New("type is not elements")
